#class login open;
#action {«Î ‰»Îœ‡”¶√‹¬Î} {
    #delay {2} {
        login;
    };
};

#alias {login} {
    #send {score};
};

#action {°æ%*°ø%* %*(%*)} {
    #var char_name %3;
    #var char_id @lower{%4};
    #class login kill;
    #send {hp};
    #class login kill;
    #delay {2} {
        show.init;        
    };
};

#class login close;
