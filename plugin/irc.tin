#class irc open;
#alias {irc.start} {
    #run {irc} {./bin/irc_bot.py ${char}};
    #unticker {%*};
    #unaction {%*};
    #action {${char}: %*} {
        #mud %%1;
    };
    #mud;
};

#alias {irc.notify} {
    #irc %0;
};

#alias {status} {
    #cr;
};

#class irc close;
